package System_tests

import org.junit._
import junit.framework.Assert._

class Sending_messages_between_hosts {

    @Before
    def when_a_multi_node_cluster_is_up_and_running_configured_with_commands_and_registered_handlers {
        assertFalse(true)
    }

    @Test
    def commands_are_routed_to_register_handler_and_will_eventually_be_available {
        assertFalse(true)
    }

    // update checklist - must check config validity every time they are updated
        // cannot have to handlers of the same command

}
